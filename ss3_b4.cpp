#include <stdio.h>
int main(){
    int so1,so2,so3;
    scanf("%d", &so1);
    scanf("%d", &so2);
    scanf("%d", &so3);
    if((so3>so1) && (so3<so2)) printf("so thu 3 nam giua so thu 2 va so thu 1");
    else printf("so thu 3 khong nam giua so thu 2 va so thu 1");
	  }